# Database mode switch
# Default (if unset): pglite
DB_MODE=pglite

# Optional aliases:
# DATABASE_MODE=external-postgres
# USE_EXTERNAL_DB=true

# Required when DB_MODE=external-postgres (or USE_EXTERNAL_DB=true)
# DATABASE_URL_EXTERNAL=postgresql://postgres:postgres@127.0.0.1:5432/hospital

# PGlite settings (used only in pglite mode)
PGLITE_PORT=5433

# setup.js runs by default in pglite mode.
# Set to false to skip.
PGLITE_RUN_SETUP=true

# Optional build guardrails.
# If set, prepare-resources fails unless the source repo is on that branch.
# EXPECTED_BACKEND_BRANCH=main
# EXPECTED_FRONTEND_BRANCH=main

# Auto-update
# In production packaged app, update checks run automatically.
# Set true only if you need to test updater flow in dev mode.
ENABLE_DEV_UPDATER=false
